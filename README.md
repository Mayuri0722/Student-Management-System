# Student-Management-System
The Student Management System is a Spring Boot web app that manages student records with CRUD operations. It uses Spring MVC, Thymeleaf, and MySQL for data storage, offering an interactive Bootstrap-based interface for adding, editing, viewing, and deleting student details efficiently.
